name: cockpit_tools
description: "Cockpit Tools Mobile — AI IDE Account & Quota Manager"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.7.0


dependencies:
  flutter:
    sdk: flutter

  # Supabase
  supabase_flutter: ^2.9.0

  # State Management
  flutter_riverpod: ^3.2.1

  # Secure Storage
  flutter_secure_storage: ^10.0.0

  # UI & Design
  google_fonts: ^8.0.2
  percent_indicator: ^4.2.3
  shimmer: ^3.0.0

  # Utilities
  intl: ^0.20.2
  uuid: ^4.5.1
  shared_preferences: ^2.5.4
  flutter_svg: ^2.2.3
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/icons/
