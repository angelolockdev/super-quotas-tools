---
description: Set up the Cockpit Tools development environment
---

1. Ensure Flutter is installed and `flutter doctor` passes
2. Sync dependencies:
// turbo
3. Run `flutter pub get`
4. Set up Supabase credentials in `.env` (copy from `.env.example`)
5. Run the app:
6. Run `flutter run`
